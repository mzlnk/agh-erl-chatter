{erl_opts, [debug_info]}.
{deps, []}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [erlchatter]}
]}.

{relx, [{release, {erlchatter, "0.1"},
         [erlchatter]},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]}.
